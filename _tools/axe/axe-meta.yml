# Enable Quarto's built-in axe-core reporter for a whole-site audit render.
# Apply at render time WITHOUT editing _quarto.yml:
#   quarto render --metadata-file _tools/axe/axe-meta.yml
format:
  html:
    axe:
      output: json
