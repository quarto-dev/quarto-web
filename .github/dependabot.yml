version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    cooldown:
      default-days: 7 # don't adopt a release the day it ships
    groups:
      actions:
        patterns: ["*"]
