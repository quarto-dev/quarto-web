# Legacy gallery

`gallery.ejs` and `gallery.yml` are the previous gallery implementation, kept only as the worked example for the custom-listings documentation.

`docs/websites/website-listings-custom.qmd` links to both files on GitHub as the reference template and metadata for a custom listing.

Do not delete this directory.
Removing it would break those links, even though nothing here is rendered as part of the site.
