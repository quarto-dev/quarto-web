---
title: "Website Accessibility Checks"
---

{{< prerelease-callout 1.11 >}}

::: {.callout-warning title="Experimental command"}
`quarto call axe` is experimental. The command is hidden, so it does not appear in `quarto call --help`, and it carries no stability promise. Flags, artifacts, and behavior can change between pre-release builds.

Tell us how it works for you in [Quarto discussions](https://github.com/quarto-dev/quarto-cli/discussions).
:::

## Overview

`quarto call axe` scans a rendered Quarto site for accessibility violations with [`axe-core`](https://github.com/dequelabs/axe-core). It drives headless Chrome over every page of the site, at a desktop width and a mobile width, in each color mode the page ships.

The command groups violations by root cause and compares them against a baseline that you commit. It then writes a report. You can read that report, post it into a pull request, or use it to fail a CI job.

## When to Use a Site Scan

Quarto gives you two ways to check accessibility, and they answer different questions.

The [`axe` format option](/docs/output-formats/html-accessibility.qmd) checks the page you are looking at, in your own browser, while you write it. Use it for fast feedback on one page.

`quarto call axe` checks the whole site: every page, both widths, and light and dark mode. It runs after a render, or in CI. Use it when you want a site-wide inventory of problems, a gate that catches new problems, or output that an agent can work through.

## Prerequisites

The scan needs a Chromium browser that Quarto can find. Install the browser that Quarto manages:

``` {.bash filename="Terminal"}
quarto install chrome-headless-shell
```

An installed system Chrome or Edge also works.

## Your First Scan

Render the site, then scan the output directory:

``` {.bash filename="Terminal"}
quarto render
quarto call axe _site
```

The scan prints the matrix it covers, then one line for each cell, then a summary of the findings.

A cell is one page, at one width, in one color mode. A page that ships a light mode and a dark mode contributes two cells. A page with a single mode contributes one.

## Scan Artifacts

The scan writes its artifacts to `_axe-checks/` at the project root. The project root is the directory of the nearest `_quarto.yml` at or above the site directory.

| File | What it is |
|--------------|-----------------------------------------------------------|
| `findings.json` | Every finding, machine-readable. |
| `report.md` | The summary for people to read, in GitHub-flavored markdown. |
| `README.md` | Generated documentation for the artifacts, including how to write a baseline. |
| `cells/` | The raw `axe-core` output for each page, width, and mode. |

The `_axe-checks/` directory ignores itself. The scanner writes a `.gitignore` with `*` into it, because the directory is a disposable snapshot and not a thing to commit.

Read the generated `README.md` after a scan. It documents the shape of every entry in `findings.json`, and it always matches the build that wrote it.

Each finding carries an id that is stable across runs, such as `image-alt-6e3b76`. The id works as a handle in a task list, or in an instruction to an agent that reads the artifacts for you.

Fixes belong in your Quarto source: a `.qmd` file, `_quarto.yml`, `_brand.yml`, or a theme `.scss` file. Do not edit the rendered site directory, because the next render overwrites it.

## Scan Options

| Flag | Default | Description |
|-------------|-------------|-------------------------------------------|
| `--pages <globs>` | all `*.html` | Comma-separated globs, relative to the site directory. |
| `--exclude <globs>` | — | Globs to skip, applied after `--pages`. |
| `--max-pages <n>` | ∞ | A cap on the number of pages. The pages are sorted first, so the cap is deterministic. Redirect stubs do not use up the cap. |
| `--viewports <WxH,...>` | `1440x900,320x568` | The widths and heights to scan. |
| `--themes <light,dark>` | `light,dark` | Which color modes to scan. This filters pages with two modes. A page with a single mode always scans once. |
| `--timeout <ms>` | `30000` | The time budget for one cell. |
| `--settle <ms>` | `50` | An extra delay after the page reports that it is ready. |
| `--fail-on <impact>` | off | Exit 1 when there are new findings at or above `minor`, `moderate`, `serious`, or `critical`. |
| `--report <path>` | `_axe-checks/report.md` | Write the report somewhere else, such as inside your site source. |

The narrow default width is 320 CSS pixels. This is the width that WCAG's reflow criterion (SC 1.4.10) names, and it is equivalent to 400% zoom on a 1280 pixel window. Every rule therefore runs against the reflowed mobile layout of your site.

If you scan a subset with `--pages`, `--exclude`, or `--max-pages`, the artifacts say so. Their counts describe the subset, not the site.
