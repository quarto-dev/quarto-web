[project]
name = "quarto-demo"
version = "0.1.0"
description = "Quarto demo site with Python and R"
requires-python = ">=3.12"
dependencies = [
    "jupyter",
    "matplotlib",
    "pandas",
    "tabulate",
    "ipyleaflet",
]
